# Vendored Package Copy

This directory is a repository-local copy of `@midnight-ntwrk/midnight-did-credentials-status-registry`, synced from:

- source repo: `midnight-verifiable-credentials`
- workspace: `registry/status-registry`

Refresh it with:

```bash
./upgrade-libs.sh --destination .
```
