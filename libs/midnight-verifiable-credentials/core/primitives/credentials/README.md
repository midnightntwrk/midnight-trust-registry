# Vendored Package Copy

This directory is a repository-local copy of `@midnight-ntwrk/midnight-did-credentials`, synced from:

- source repo: `midnight-verifiable-credentials`
- workspace: `core/primitives/credentials`

Refresh it with:

```bash
./upgrade-libs.sh --destination .
```
