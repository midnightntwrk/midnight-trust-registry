# Vendored Package Copy

This directory is a repository-local copy of `@midnight-ntwrk/midnight-did-jubjub-schnorr`, synced from:

- source repo: `midnight-did`
- workspace: `jubjub-schnorr`

Refresh it with:

```bash
./upgrade-libs.sh --destination .
```
