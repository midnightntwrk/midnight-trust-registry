# Vendored Package Copy

This directory is a repository-local copy of `@midnight-ntwrk/midnight-did-domain`, synced from:

- source repo: `midnight-did`
- workspace: `domain`

Refresh it with:

```bash
./upgrade-libs.sh --destination .
```
