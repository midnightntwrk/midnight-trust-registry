# Vendored Package Copy

This directory is a repository-local copy of `@midnight-ntwrk/midnight-did-contract`, synced from:

- source repo: `midnight-did`
- workspace: `contract`

Refresh it with:

```bash
./upgrade-libs.sh --destination .
```
