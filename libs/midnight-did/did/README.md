# Vendored Package Copy

This directory is a repository-local copy of `@midnight-ntwrk/midnight-did`, synced from:

- source repo: `midnight-did`
- workspace: `did`

Refresh it with:

```bash
./upgrade-libs.sh --destination .
```
